# Netlify Redirects for mrdanish.netlify.app
# SPA fallback for React Router

# Redirect www to non-www (if custom domain)
# https://www.mrdanish.netlify.app/* https://mrdanish.netlify.app/:splat 301!

# SPA fallback - send all routes to index.html
/*    /index.html   200
